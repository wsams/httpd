name: CI

on:
  pull_request:
  push:
    branches:
      - master

permissions:
  contents: read

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    env:
      IMAGE_NAME: wsams/httpd
    steps:
      - name: Check out repository
        uses: actions/checkout@v7.0.1

      - name: Build all image flavors
        run: |
          chmod +x scripts/build-images.sh scripts/test-images.sh
          IMAGE_NAME="${IMAGE_NAME}" ./scripts/build-images.sh ci

      - name: Smoke-test image flavors
        run: |
          IMAGE_NAME="${IMAGE_NAME}" ./scripts/test-images.sh ci
